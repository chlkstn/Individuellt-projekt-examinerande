import React, {
  createContext,
  useState,
  useContext,
  useEffect,
  ReactNode,
} from "react";
import { Visitor } from "./Interface"; 


const VisitorContext = createContext(undefined);





export const VisitorProvider: React.FC = ({ children }) => {
    
    });


